# Supabase on Vercel provides these automatically. POSTGRES_URL is pooled for
# Vercel Functions; POSTGRES_URL_NON_POOLING is used by migration tooling.
POSTGRES_URL=
POSTGRES_URL_NON_POOLING=
DATABASE_URL=
SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Clerk controls Google and GitHub SSO in the hosted website.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Optional OpenFGA fine-grained workspace authorization.
OPENFGA_API_URL=
OPENFGA_STORE_ID=
OPENFGA_AUTHORIZATION_MODEL_ID=
OPENFGA_CLIENT_TOKEN=
# Auth0 FGA hosted credentials (use these instead of OPENFGA_CLIENT_TOKEN).
OPENFGA_API_TOKEN_ISSUER=https://auth.fga.dev
OPENFGA_API_AUDIENCE=
OPENFGA_CLIENT_ID=
OPENFGA_CLIENT_SECRET=

# Reserved for Phase 5 realtime coordination.
REDIS_URL=

# Hocuspocus/Yjs realtime collaboration. The token secret must be shared by
# the Vercel web app and the standalone Hocuspocus service.
HOCUSPOCUS_TOKEN_SECRET=
NEXT_PUBLIC_HOCUSPOCUS_URL=ws://localhost:8787

# Phase 2 GitHub App authorization. Generate AUTH_SECRET with `pnpm auth:secret`
# and CREDENTIAL_ENCRYPTION_KEY with `openssl rand -base64 32`.
AUTH_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
# Google OAuth callback: /api/auth/callback/google
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
# Transactional From address (Resend). Defaults to CoDev <noreply@trycodev.com>.
AUTH_EMAIL_FROM=
# Waitlist pings go here. Use the ImprovMX alias, e.g. yousef@trycodev.com.
ACCESS_REQUEST_NOTIFY_EMAIL=
# Comma-separated emails that can create an account without a waitlist invite.
SIGNUP_ALLOWLIST=
GITHUB_APP_SLUG=
# Fine-grained or classic PAT with issues:write on the CoDev product repo.
# Used by the floating feedback widget to open GitHub issues automatically.
FEEDBACK_GITHUB_TOKEN=
# Optional override; defaults to VERCEL_GIT_REPO_OWNER/SLUG or yousef20920/CoDev.
FEEDBACK_GITHUB_REPO=
CREDENTIAL_ENCRYPTION_KEY=
# Required in production for AWS KMS-backed GitHub token storage.
CREDENTIAL_KMS_KEY_ID=

# Optional subscription OAuth overrides for Claude Code and Codex.
# Defaults use the public Claude Code / Codex CLI PKCE clients:
# Claude = paste-code via Anthropic's callback page
# Codex = ChatGPT device-code sign-in
# Set *_OAUTH_REDIRECT_URI to switch a provider into app-callback mode with
# /api/auth/oauth/claude/callback or /api/auth/oauth/codex/callback.
CLAUDE_OAUTH_CLIENT_ID=
CLAUDE_OAUTH_CLIENT_SECRET=
CLAUDE_OAUTH_AUTHORIZE_URL=
CLAUDE_OAUTH_TOKEN_URL=
CLAUDE_OAUTH_REDIRECT_URI=
CLAUDE_OAUTH_SCOPE=
CODEX_OAUTH_CLIENT_ID=
CODEX_OAUTH_CLIENT_SECRET=
CODEX_OAUTH_AUTHORIZE_URL=
CODEX_OAUTH_TOKEN_URL=
CODEX_OAUTH_REDIRECT_URI=
CODEX_OAUTH_SCOPE=

# OpenAI model used for agent runs and AI-generated pull request summaries.
# Defaults to gpt-5 when omitted.
CODEV_OPENAI_MODEL=

# Phase 3 Vercel-to-AWS orchestration. AWS_ROLE_ARN is assumed with the
# short-lived Vercel OIDC token; never add an AWS access key to Vercel.
AWS_REGION=us-east-2
AWS_ROLE_ARN=
AWS_HOST_INSTANCE_ID=
ORCHESTRATOR_URL=

# Authenticates Vercel's Phase 8 lifecycle reconciler.
CRON_SECRET=
